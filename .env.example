# ==============================================
# Hennesy Frontend - Variables de Entorno
# ==============================================
# Copiar a .env y ajustar valores

# URL del backend API (servidor separado)
# En producción, usar el dominio público del backend
VITE_API_BASE_URL=https://api.tudominio.com

# Puerto expuesto al host (para Cloudflare Tunnel)
FRONTEND_PORT=8080
